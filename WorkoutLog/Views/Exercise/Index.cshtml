@model System.Collections.Generic.IEnumerable<WorkoutLog.Models.Exercise>

@{
    ViewBag.Title = "Atlas ćwiczeń";
}

<br/>
<a href="@Url.Action("Create", "Exercise")">
    <button type="button" class="btn btn-default btn-lg">
        Dodaj nowe <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
    </button>
</a>
<br/>
<br />
<div>
    <table id="example" class="table table-striped table-bordered" cellspacing="0" width="60%">
        <thead>
            <tr>
                <th>Nazwa</th>
                <th>Opis ćwiczenia</th>
                <th>Opcje</th>
            </tr>
        </thead>

        <tbody>
        @foreach (var exercises in Model)
        {
            <tr>
                <td>@exercises.ExerciseName</td>
                <td>@exercises.ExerciseDesc</td>
                <td><a href="@Url.Action("Delete", "Exercise", new {id = exercises.ExerciseId})">
                    <button type="button" class="btn btn-default">Usuń <span class="glyphicon glyphicon-remove"></span></button>
                </a></td>
            </tr>
        }
        </tbody>
    </table>
</div>